#include "holberton.h"
/**
* _strlen - function length of a string
*
* @s: check pointer string
*
* Return: length of a string
**/
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
