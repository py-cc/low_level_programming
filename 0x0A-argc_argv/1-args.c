#include <stdio.h>
#include "holberton.h"
/**
* main - Entry point
* @argc: size of argv
* @argv: size of command arguments
* Return: 0 Sucess
*/

int main(int argc, char *argv[] __attribute__((unused)))
{

	int i;

	for (i = 0; i < argc; i++)
	{
	}

	i = i - 1;

	printf("%d\n", i);
	return (0);
}
